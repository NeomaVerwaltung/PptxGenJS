{
	"name": "pptxgenjs-demos",
	"version": "4.0.1",
	"private": true,
	"type": "module",
	"description": "Local dev/demo harness for PptxGenJS (not published). Run `npm install` here before `node browser_server.mjs`.",
	"license": "MIT",
	"dependencies": {
		"express": "^5.1.0"
	}
}
